function simulateXiaolinSeason(rangeOfEntrants, seasonLength) {
  // uses ladder matchmaking
  // track length of reign, and who each titleholder (the #1 team) faces
  // this will be needed for potential tiebreakers at the end of the season

  // default to 37 matchdays if not specified
  var matchdays = seasonLength === undefined ? 37 : seasonLength;

}
