/**
 * Generates a series of fixtures for a UEFA Champions League style "League Phase" (a modified Swiss System Tournament)
 *
 * @param {Array<Array<string|number>>} rangeOfEntrants A range containing RankPts, Names, and Nations, in that order.
 * @param {number} numberOfRounds The number of rounds (matchdays) there should be
 * @return An array of entrants and their matchups
 * @customfunction
 */
function uclLeaguePhaseDraw(rangeOfEntrants, numberOfRounds) {
  
}
